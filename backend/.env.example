# Database connection string using asyncpg driver
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/greencare

# Optional: enable SQLAlchemy echo for debugging
SQL_ECHO=false

# Comma-separated API tokens for bin telemetry ingestion
TELEMETRY_API_TOKENS=telemetry-token
